import { describe,it,expect } from 'vitest';import { distanceBetween,calculateStatistics } from '../statistics/calculate';import { createActivityId } from '../identifiers/createActivityId';import { parseGpx } from '../formats/gpx/parseGpx';import { serializeActivity } from '../rdf/serializeActivity';import { Parser } from 'n3';
describe('conversion core',()=>{it('does not bridge segments and creates stable identifiers',async()=>{const xml=`<gpx><trk><name>Ride</name><type>bike</type><trkseg><trkpt lat="60" lon="24"><time>2026-07-14T09:16:00Z</time></trkpt><trkpt lat="60" lon="24.01"><time>2026-07-14T09:16:10Z</time></trkpt></trkseg><trkseg><trkpt lat="61" lon="25"/></trkseg></trk></gpx>`;const a=parseGpx(new TextEncoder().encode(xml),'test.gpx','a'.repeat(64));const s=calculateStatistics(a);expect(s.distanceMeters).toBeCloseTo(distanceBetween(a.tracks[0].segments[0].points[0],a.tracks[0].segments[0].points[1]));expect(createActivityId('a'.repeat(64),s.startTime)).toBe('20260714T091600Z-aaaaaaaaaaaa');const ttl=await serializeActivity(a,s,'../../source-files/2026/x.gpx');expect(new Parser().parse(ttl).length).toBeGreaterThan(0)})})
