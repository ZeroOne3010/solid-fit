import { describe,it,expect } from 'vitest';import { distanceBetween,calculateStatistics } from '../statistics/calculate';import { createActivityId } from '../identifiers/createActivityId';import { parseGpx } from '../formats/gpx/parseGpx';import { serializeActivity } from '../rdf/serializeActivity';import { Parser } from 'n3';import { summarizeActivities } from '../app/importSummary';
describe('conversion core',()=>{it('does not bridge segments and creates stable identifiers',async()=>{const xml=`<gpx><trk><name>Ride</name><type>bike</type><trkseg><trkpt lat="60" lon="24"><time>2026-07-14T09:16:00Z</time></trkpt><trkpt lat="60" lon="24.01"><time>2026-07-14T09:16:10Z</time></trkpt></trkseg><trkseg><trkpt lat="61" lon="25"/></trkseg></trk></gpx>`;const a=parseGpx(new TextEncoder().encode(xml),'test.gpx','a'.repeat(64));const s=calculateStatistics(a);expect(s.distanceMeters).toBeCloseTo(distanceBetween(a.tracks[0].segments[0].points[0],a.tracks[0].segments[0].points[1]));expect(createActivityId('a'.repeat(64),s.startTime)).toBe('20260714T091600Z-aaaaaaaaaaaa');const ttl=await serializeActivity(a,s,'../../source-files/2026/x.gpx');expect(new Parser().parse(ttl).length).toBeGreaterThan(0)})})
describe('import feedback',()=>{it('explains why an invalid GPX has no usable points',()=>{expect(()=>parseGpx(new TextEncoder().encode('<gpx><trk><trkseg><trkpt lat="200" lon="24"/></trkseg></trk></gpx>'),'bad.gpx','b'.repeat(64))).toThrow('All 1 track point was rejected because its latitude or longitude is invalid.')});it('summarizes distinct types and dated years',()=>{expect(summarizeActivities([{id:'a',type:'Cycling',distance:0,warnings:0,start:new Date('2024-01-01')},{id:'b',type:'Cycling',distance:0,warnings:0,start:new Date('2025-01-01')},{id:'c',type:'Running',distance:0,warnings:0}])).toEqual({exerciseTypes:2,yearsCovered:2})})})
